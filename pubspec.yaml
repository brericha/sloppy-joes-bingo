name: fk_bingo
description: Sloppy Joe's Bingo
publish_to: 'none'
version: 0.0.1+1

environment:
  sdk: '>=3.0.2 <4.0.0'

dependencies:
  auto_size_text: ^3.0.0
  confetti: ^0.7.0
  flutter:
    sdk: flutter
  responsive_sizer: ^3.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  generate: true